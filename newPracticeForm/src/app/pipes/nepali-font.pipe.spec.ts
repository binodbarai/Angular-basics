import { NepaliFontPipe } from './nepali-font.pipe';

describe('NepaliFontPipe', () => {
  it('create an instance', () => {
    const pipe = new NepaliFontPipe();
    expect(pipe).toBeTruthy();
  });
});
